package com.ulegalize.enumeration;

/**
 * The enum Enum context template.
 */
public enum EnumContextTemplate {
    /**
     * Cabinet enum context template.
     */
    CABINET,
    /**
     * Facture enum context template.
     */
    FACTURE,
    /**
     * Dossier enum context template.
     */
    DOSSIER;
}
