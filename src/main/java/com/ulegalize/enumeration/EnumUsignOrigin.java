package com.ulegalize.enumeration;

/**
 * The enum Enum usign origin.
 */
public enum EnumUsignOrigin {
    /**
     * From case enum usign origin.
     */
    FROM_CASE,
    /**
     * From channel enum usign origin.
     */
    FROM_CHANNEL,
    /**
     * From vckey enum usign origin.
     */
    FROM_VCKEY;
}
